- name: mysql config
  hosts: mysql
  become: yes
  tasks:
  - name: install mysql server
    ansible.builtin.package:
      name: mysql-server
      state: present
  
  - name: start my sql
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes

